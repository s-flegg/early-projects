# methods such as decision trees can be prone to overfitting
# bagging (Bootstrap Aggregation) is an ensembling method
# attempts to resolve overfitting

# takes a random subset of original data, with replacement,
# and fits either a classifier or regressor to each subset

